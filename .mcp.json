{
  "mcpServers": {
    "json-inspector": {
      "command": "uv",
      "args": ["run", "${CLAUDE_PLUGIN_ROOT}/scripts/server.py"],
      "env": {
        "JSON_INSPECTOR_SAMPLES": "${JSON_INSPECTOR_SAMPLES}",
        "JSON_INSPECTOR_SCHEMAS": "${JSON_INSPECTOR_SCHEMAS}"
      }
    }
  }
}
